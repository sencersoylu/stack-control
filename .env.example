# Stack Control Environment Variables

# Cloud Sync Configuration (optional)
# Set these to enable cloud sync to Supabase
CLOUD_API_URL=http://localhost:3100
CHAMBER_API_KEY=CHB-001-your-api-key-here

# If CLOUD_API_URL and CHAMBER_API_KEY are not set,
# cloud sync will be disabled and the system will
# work in offline-only mode.
